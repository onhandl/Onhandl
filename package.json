{
  "name": "Onhandl-root",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development pnpm --filter \"*\" dev",
    "build": "pnpm --filter \"*\" build",
    "start": "NODE_ENV=production pnpm --filter \"*\" start",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}